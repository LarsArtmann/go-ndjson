# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

> A feature earns `FULLY_FUNCTIONAL` only when you can point to the code that
> delivers it AND confirm it works. If you're unsure, it's `PARTIALLY_FUNCTIONAL`
> at best, never round up.

## NDJSON Reader

| Feature                           | Status                | Notes                                                                |
| --------------------------------- | --------------------- | -------------------------------------------------------------------- |
| Generic typed parsing (`Read[T]`) | 🟢 `FULLY_FUNCTIONAL` | `reader.go:33`; tested in `reader_test.go`, fuzzed in `fuzz_test.go` |
| Blank line skipping               | 🟢 `FULLY_FUNCTIONAL` | `reader.go:45`; tested in `TestRead_BlankLinesSkipped`               |
| Per-line validation callback      | 🟢 `FULLY_FUNCTIONAL` | `reader.go:56-61`; tested in `TestRead_ValidationRejects*`           |
| Nil validator skip                | 🟢 `FULLY_FUNCTIONAL` | `reader.go:56`; tested in `TestRead_NilValidatorSkipsValidation`     |
| Carriage return handling          | 🟢 `FULLY_FUNCTIONAL` | `reader.go:43` (bufio.Scanner); tested in `TestRead_CarriageReturns` |
| No trailing newline handling      | 🟢 `FULLY_FUNCTIONAL` | Tested in `TestRead_NoTrailingNewline`                               |
| 1 MB line size cap                | 🟢 `FULLY_FUNCTIONAL` | `reader.go:13` (`MaxLineBytes`); tested in `TestRead_OversizedLine`  |
| `ErrEmpty` sentinel (no data)     | 🟢 `FULLY_FUNCTIONAL` | `reader.go:16`; tested in `TestRead_EmptyInput`                      |
| `ErrNoEvents` sentinel (blank)    | 🟢 `FULLY_FUNCTIONAL` | `reader.go:19`; tested in `TestRead_OnlyBlankLines`                  |
| `ErrOversizedLine` sentinel       | 🟢 `FULLY_FUNCTIONAL` | `reader.go:22`; tested in `TestRead_OversizedLine`                   |
| Line-numbered parse errors        | 🟢 `FULLY_FUNCTIONAL` | `reader.go:53`; tested in `TestRead_MalformedJSON`                   |

## Format Detection (loader package)

| Feature                          | Status                | Notes                                                                                                                 |
| -------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------- |
| JSON Report detection            | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:103-105`; key-presence probe (any `"version"` key, incl. empty/null); `TestDetect_KeyPresence`      |
| NDJSON Event detection           | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:99-101`; key-presence probe; `"event_type"` wins over `"version"` when both present                |
| Multi-line JSON fallback         | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:87-90`; unparseable first line is a report guess only when it starts with `{`                       |
| Ambiguous input rejection        | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:92-96,107-111`; neither key, non-object, or text fails with `ErrUnknownFormat`; `TestDetect_UnknownFormat` |
| Blank line skipping before probe | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:66-71`; tested in `TestDetect_BlankLinesBeforeContent`                                              |
| `ErrNoContent` sentinel          | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:16`; tested in `TestDetect_EmptyInput`                                                              |
| `ErrUnknownFormat` sentinel      | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:21`; errors carry a truncated line preview; `TestDetect_UnknownFormatErrorPreviewsLine`             |
| Oversized first line error       | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:73-76`; wraps `bufio.ErrTooLong`; tested in `TestDetect_OversizedLine`                              |
| `Format.String()` method         | 🟢 `FULLY_FUNCTIONAL` | `loader/format.go:36`; tested in `TestFormat_String`                                                                  |

## Tests & Benchmarks

| Feature                | Status                | Notes                                                                           |
| ---------------------- | --------------------- | ------------------------------------------------------------------------------- |
| Fuzz test (`FuzzRead`) | 🟢 `FULLY_FUNCTIONAL` | `fuzz_test.go`; reader never panics, never returns events alongside error       |
| Benchmarks             | 🟢 `FULLY_FUNCTIONAL` | `BenchmarkRead` (`reader_test.go`), `BenchmarkDetect` (`loader/format_test.go`) |

## Project Infrastructure

| Feature             | Status                | Notes                                                                             |
| ------------------- | --------------------- | --------------------------------------------------------------------------------- |
| GitHub Actions CI   | 🟡 `PARTIALLY_FUNCTIONAL` | `.github/workflows/ci.yml`; first run 2026-09-07 failed at lint (system Go rejected `GOEXPERIMENT=jsonv2`); root cause fixed in `flake.nix:109-113`, green run pending push |
| Flake quality gates | 🟢 `FULLY_FUNCTIONAL` | `flake.nix` apps: test, test-race, build, vet, lint, coverage, vulncheck          |
